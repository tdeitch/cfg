#! /usr/bin/env python2

# urlencode
# URL escape all required characters.
# If replace_plus is true, replaces spaces with `+`.
replace_plus=True

import sys, urllib
if replace_plus:
    print urllib.quote_plus(' '.join(sys.argv[1:]))
else:
    print urllib.quote(' '.join(sys.argv[1:]))
